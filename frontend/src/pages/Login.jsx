import React, { useState } from 'react';
import { useLocation, useNavigate } from 'react-router-dom';
import { useAuth } from '../hooks/useAuth.jsx';

export default function Login(){const {login,loading,error}=useAuth();const nav=useNavigate();const loc=useLocation();const [email,setEmail]=useState('user@passabola.com');const [password,setPassword]=useState('123456');const submit=async e=>{e.preventDefault();try{await login(email,password);nav(loc.state?.from?.pathname||'/')}catch{}};return <div className="min-h-[75vh] grid place-items-center px-4 py-12"><form onSubmit={submit} className="w-full max-w-md bg-white border rounded-3xl shadow-lg p-8"><h1 className="text-3xl font-black text-purple-800">Entrar</h1><p className="text-slate-500 mt-1">Acesse o Fantasy League.</p>{error&&<div className="mt-5 p-3 rounded-lg bg-red-50 text-red-700 text-sm">{error}</div>}<label className="block mt-6 text-sm font-bold">Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" className="w-full mt-2 border rounded-xl p-3"/></label><label className="block mt-4 text-sm font-bold">Senha<input value={password} onChange={e=>setPassword(e.target.value)} type="password" className="w-full mt-2 border rounded-xl p-3"/></label><button disabled={loading} className="w-full mt-6 rounded-xl p-3 text-white font-bold header-brand">{loading?'Entrando...':'Entrar'}</button><p className="mt-5 text-xs text-slate-500">Demo: user@passabola.com / 123456</p></form></div>}
